<h1>Book</h1>
<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <% @book_id.each do |book| %>
  <tboby>
    <tr>
      <td>a</td>
      <td><%= link_to book.title, book_path(book.id) %></td>
      <td><%= book.body %></td>
    </tr>
  </tboby>
  <% end %>
</table>
